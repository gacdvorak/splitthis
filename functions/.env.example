# SMTP Configuration
# For Gmail: Use smtp.gmail.com with an App Password
# For SendGrid: Use smtp.sendgrid.net with your API key as password
# For other providers: Use their SMTP settings

# SMTP Host (default: smtp.gmail.com)
SMTP_HOST=smtp.gmail.com

# SMTP Port (default: 587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP Secure (true for port 465, false for other ports)
SMTP_SECURE=false

# SMTP Username (your email address for Gmail, or 'apikey' for SendGrid)
SMTP_USER=your-email@gmail.com

# SMTP Password (App Password for Gmail, API key for SendGrid)
SMTP_PASS=your-app-password-here

# From Email Address
FROM_EMAIL=SplitThis <your-email@gmail.com>

# Application URL
# The base URL where your app is hosted
APP_URL=https://splitthis.app
